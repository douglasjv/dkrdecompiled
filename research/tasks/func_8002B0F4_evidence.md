# func_8002B0F4 Evidence Ledger

Purpose: durable negative evidence for `func_8002B0F4` so `research/tasks/ACTIVE.md` can stay compact. Source remains active and routable; these entries prevent blind retries of saturated source-shape families.

Current compact read:
- Guarded source: `src/tracks.c` under `#ifdef NON_EQUIVALENT`.
- Original asm: `asm/nonmatchings/tracks/func_8002B0F4.s`.
- Recurring promoted-source family: current C tends to hoist `gCurrentLevelModel` before the segment loop and spill it around `0x60(sp)`, while target reloads `gCurrentLevelModel` inside the loop. Segment/grid and bottom water scheduling then drift broadly.

## Rejected Probes

- 2026-05-25 `sp108 <= 0` entry-guard spelling: changed `if (sp108 == 0 || sp108 >= 8)` to `if (sp108 <= 0 || sp108 >= 8)`. Relinked focused diff regressed from promoted baseline `CURRENT (2860)` to `CURRENT (3060)`, changed target `beqz v0` into current `blez v0`, and retained the unwanted early `gCurrentLevelModel` spill. Do not repeat this entry-guard spelling.
- Prior inner face-loop `!=` bound spelling: changed `faceNum < nextFaceOffset` to `faceNum != nextFaceOffset`. Relinked focused diff regressed to `CURRENT (3870)`, changed the inner loop family from target `slt`/`bnez` to `bne`, and reintroduced the model spill. Do not repeat loop-bound spellings.
- Prior local `LevelModel *levelModel` pressure spelling: introduced a local model pointer for segment, bounding-box, texture, and bottom segment accesses. Relinked focused diff regressed to `CURRENT (4208)`, widened the frame to `0x130`, and spilled the model pointer. Do not repeat local model-pointer pressure probes.
- Prior texture-index carriers, flags carriers, pointer-arithmetic segment setup, bottom store-order, condition-order, literal/cast, and local-width spellings repeatedly retained the early model spill family and regressed or stayed in the same broad segment/grid drift. Do not repeat those spelling families without a distinct mechanism that predicts removal of the model spill.
- 2026-05-31 worker bbox-before-segment ordering: promoted the guard and reordered initial loop setup to assign `currentBoundingBox` before `currentSegment`. Full verify failed with CRCs `0x7856718A/0xA6A743D8`; relinked focused diff regressed to `CURRENT (3965)`. The probe made the early model-load family worse by hoisting `gCurrentLevelModel` before the segment loop and spilling it to `0x60(sp)`, then reloading it; target loads `gCurrentLevelModel` inside the loop. Source was restored and restored validation reached `Verify: OK`. Do not repeat initial segment/bbox assignment-order pressure probes unless the next hypothesis specifically predicts removal of the early model stack spill.
- 2026-05-31 worker segment-index local lifetime: promoted the guard and introduced a single `segmentIndex = spB0[var_fp]` local so `currentSegment` and `currentBoundingBox` derive from the same segment id. Full verify failed with CRCs `0x7916617A/0xEAA308C1` versus expected `0x53D440E7/0x7519B011`; relinked focused diff improved to `CURRENT (2926)` but missed the intended mechanism. The frame widened from target `0x128` to current `0x130`, and the source still hoisted `gCurrentLevelModel` before the segment loop, spilled it at `0x64(sp)`, and reloaded it inside the loop instead of target-like in-loop global loads. Source was restored and restored validation reached `Verify: OK`. Do not repeat simple local segment-index lifetime variants.
- 2026-05-31 worker bottomSegment lifetime split: promoted the guard, added a separate `LevelModelSegment *bottomSegment`, and used it only in the post-loop `levelSegmentIndex` water block while leaving outer-loop `currentSegment` for traversal. Full verify failed with CRCs `0x76D9116A/0x07C0B726`; relinked focused diff regressed to `CURRENT (3123)`. The frame widened from target `0x128` to current `0x130`, `spB0` shifted from `0xB0(sp)` to `0xB4(sp)`, the early model-load family worsened by hoisting `gCurrentLevelModel` before the segment loop and spilling it at `0x64(sp)`, and the bottom block still missed target scheduling. Source was restored and restored validation reached `Verify: OK`. Do not repeat bottom-only segment-pointer lifetime splits.
- 2026-05-31 high tooling report captured the promoted baseline object slice with `gmake -B NON_MATCHING=1 MATCHDEFS='NON_MATCHING=1 NON_EQUIVALENT=1 AVOID_UB=1' CROSS=tools/binutils/mips64-elf- build/src/tracks.c.o`, then restored the generated object with matching-mode `gmake -B CROSS=tools/binutils/mips64-elf- build/src/tracks.c.o`. Target loads `spB0[var_fp]`, then reloads `gCurrentLevelModel` into `$t3` at `0x2BDD4/0x2BDD8`, derives `segmentsBoundingBoxes` from `0x8($t3)` and `segments` from `0x4($t3)`, and reloads `gCurrentLevelModel` again inside the batch loop at `0x2C020/0x2C024` for `textures`. The promoted object hoists `gCurrentLevelModel` before the segment loop at `0x5D7C/0x5D80`, spills it to `0x60(sp)` at `0x5D94`, reloads it for outer-loop setup at `0x5DA4`, and reloads the same stack-resident model at `0x5FE8` for texture lookup. No safe packet was identified. Do not probe again until a mechanism predicts removal of the `0x60(sp)` model spill and target-like global reloads at each use.
- 2026-05-31 follow-up high discovery found no safe ordinary-C mechanism-ready packet for the same model-base spill. Remaining plausible source levers collapse into rejected local segment-index, local model pointer, bottom-only segment-pointer split, assignment/order, texture/flag carrier, pointer-arithmetic setup, condition/literal/local-width, or bottom store-order families. Mechanisms likely to force reloads now would require inappropriate `volatile`, accessor calls, artificial aliasing side effects, or helper-function reshaping, none of which is a safe DKR matching packet without a sharper target-specific prediction. Useful next analysis is a promoted-object slice audit around the hoist/spill sites, not another source probe.
- 2026-05-31 promoted object-slice audit confirmed the focused false positive for the current guarded body. Promoted `build/src/tracks.c.o` with `NON_MATCHING=1 MATCHDEFS='NON_MATCHING=1 NON_EQUIVALENT=1 AVOID_UB=1'`; focused `./diff.sh func_8002B0F4 --compress-matching 2 --no-pager` reported `CURRENT (0)`, but full ROM verify failed with calculated CRCs `0x409BE602/0x659F1EB7`. Objdump still showed the known model-base spill family: `gCurrentLevelModel` is loaded at `0x5D7C/0x5D80`, spilled to `0x60(sp)` at `0x5D94`, reloaded at `0x5DA4` for outer setup, and reloaded from `0x60(sp)` at `0x5FE8` for texture lookup; target instead reloads `gCurrentLevelModel` at each use around `0x2BDD4/0x2BDD8` and `0x2C020/0x2C024`. Matching `build/src/tracks.c.o` was restored and `gmake -j4 CROSS=tools/binutils/mips64-elf-` reached `Verify: OK`. Do not trust focused `CURRENT (0)` for this packet; next work needs a mechanism that prevents the stack-resident model base without using unsafe `volatile`/accessor/helper reshaping.
- 2026-05-31 orchestration refreshed the same promoted-object slice after selector audit surfacing. Rebuilt `build/src/tracks.c.o` with `NON_MATCHING=1 MATCHDEFS='NON_MATCHING=1 NON_EQUIVALENT=1 AVOID_UB=1'`; focused `./diff.sh func_8002B0F4 --compress-matching 2 --no-pager` again reported `CURRENT (0)`, confirming this remains a stale focused-object false positive. Objdump still showed the promoted object loading `gCurrentLevelModel` before the segment loop, keeping the stack-resident model base, and reloading it at `0x5FE8` for texture lookup instead of the target in-loop global reload at `0x2C020/0x2C024`. Matching-mode `build/src/tracks.c.o` was restored. This refresh did not identify a safe source packet; do not repeat promoted-object-slice auditing for `func_8002B0F4` unless the method reaches the full ROM verify gate or names a concrete mechanism that removes the stack-resident model base without `volatile`, accessor calls, artificial aliasing side effects, or helper reshaping.
- 2026-05-31 high mechanism-discovery worker found no mechanism-ready source packet. Evidence checked: `ACTIVE.md`, selector output, `python3 tools/query_goal_state.py packet --function func_8002B0F4`, packet template, this evidence ledger, guarded C body, and target reload sites at `0x2BDD4/0x2BDD8` and `0x2C020/0x2C024`. The current C already uses direct `gCurrentLevelModel` expressions, but IDO still CSE/hoists the model base across the outer segment loop and spills it. No unrejected ordinary-C source lever was found that predicts single-use global reloads; apparent forcing mechanisms collapse into rejected unsafe `volatile`, accessor, artificial aliasing, or helper reshaping. A valid future packet must remove the promoted stack-resident model base at `0x60(sp)`, replace the texture lookup reload from `0x5FE8` with an in-loop global `lui/lw gCurrentLevelModel` pair like target `0x2C020/0x2C024`, and preserve the outer setup global reload around `0x2BDD4/0x2BDD8`. Stop before source edits unless a new mechanism predicts those reload movements and is distinct from rejected families.

Next useful work needs discovery/tooling or a distinct model-load lifetime/register-allocation mechanism that predicts single-use `gCurrentLevelModel` reloads at outer setup and batch-loop texture lookup, removing the `0x60(sp)` stack-resident model base. Do not repeat ordering, condition, literal, local-index, carrier spelling, local model pointer, or bottom-only segment-pointer lifetime splits.
