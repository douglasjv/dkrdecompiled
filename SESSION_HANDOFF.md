# Session Handoff

- Generated at: 2026-05-16T18:27:27Z
- Branch: `master`
- HEAD before closeout commit: `03a36cc6`
- Completed task: `DKR-MATCH-FUNC-80049794-STAGED-BRANCH-CONSTANT-PROBE`
- Summary: No new source match landed. This pass kept selector-recommended
  `func_80049794` active and tested one unrepeated player-index branch spelling.
  Staging `PLAYER_COMPUTER` through the existing `var_v1` local compiled but
  left the promoted focused score unchanged at `CURRENT (2550)`, did not swap
  the target/current branch operands, and did not move the `$f14/$f20`
  allocation. The guarded matching source was restored and the full ROM gate
  remains clean.

## Validation

- `python3 tools/query_goal_state.py next --compact --refresh` -> recommends `func_80049794`; 4 default candidates, 3 exhausted notes skipped
- `python3 tools/check_active_surface.py` -> active surface ok
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with `PLAYER_COMPUTER` staged through `var_v1`
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> staged branch constant variant (`var_v1 = PLAYER_COMPUTER; if ((var_v0 == var_v1) && (gCurrentPlayerIndex != var_v1))`) leaves the focused score unchanged at `CURRENT (2550)`, does not swap target/current branch operands, and still lacks target `$f20/$f21` saves / early `$f14`
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `python3 tools/query_goal_state.py next --compact --refresh` -> recommends `func_80049794`; 4 default candidates, 3 exhausted notes skipped
- `python3 tools/check_active_surface.py` -> active surface ok
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and early zero in `$f16` instead of target `$f14`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with buoyancy `-1.0f` carrier routed through `var_f0`
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> `var_f0 = -1.0f; ... var_f20 = var_f0 - (var_f2 / 10)` leaves the focused score unchanged at `CURRENT (2550)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with player-index condition commuted and then with explicit inverted empty-branch spelling
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> both simple player-index branch source spellings leave the focused score unchanged at `CURRENT (2550)` and do not swap target/current branch operands
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and early zero in `$f16` instead of target `$f14`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with in-place inverse-gravity spelling
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> `var_f20 /= 4.0; var_f20 = 1.0 - var_f20` worsens the focused score to `CURRENT (3435)`, nonmatching, and still does not introduce target `$f20/$f21` prologue saves
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` baseline C candidate compiles
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> baseline promoted `func_80059208` remains `CURRENT (870)` in the final lateral/vertical offset block
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with the checkpoint dot accumulated through `pad2` before the object dot
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> `pad2 = tempZ * diffZ; pad2 += diffX * tempX; pad2 = -pad2` leaves the focused object score unchanged at `CURRENT (870)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with first speed-magnitude pre-`sqrtf` `var_f20` accumulation, both leading pads removed, and both trailing pads removed
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> combined leading/trailing pad removal keeps `$f20/$f21` saves but shrinks the frame to `0xf0` and remains nonmatching at `CURRENT (3737)`
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and early zero in `$f16` instead of target `$f14`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with grounded-wheel zero stores spelled as integer literals (`0`)
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> integer-literal grounded-wheel zeroing worsens to `CURRENT (4685)` and splits the two zero stores across `$f4`/`$f6`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with first speed-magnitude pre-`sqrtf` `var_f20` accumulation and both trailing unused pads removed
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> both trailing pads removed keeps target `0xf8` frame and `$f20/$f21` saves but remains nonmatching at `CURRENT (3620)`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with first speed-magnitude pre-`sqrtf` `var_f20` accumulation and only trailing `pad4` retained
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> trailing `pad4` only widens the frame to `0x100` and regresses to `CURRENT (4049)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with first speed-magnitude pre-`sqrtf` `var_f20` accumulation and only trailing `pad3` retained
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> trailing `pad3` only also widens the frame to `0x100` and regresses to `CURRENT (4049)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and early zero in `$f16` instead of target `$f14`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with leading pads combined as `UNUSED s32 pad[2]`
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> combined-pad declaration produces no object change from promoted baseline and leaves the focused score unchanged at `CURRENT (2550)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with grounded-wheel zero stores spelled as double literals (`0.0`)
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> double-literal grounded-wheel zeroing changes the early zero allocation shape but worsens the focused object score to `CURRENT (4685)` and still lacks target `$f20/$f21` saves, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and early zero in `$f16` instead of target `$f14`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with first speed-magnitude pre-`sqrtf` `var_f20` accumulation and only `pad7` retained
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> one-pad `pad7` variant introduces target `$f20/$f21` saves but keeps frame widened to `0x100` and worsens the focused object score to `CURRENT (4125)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with first speed-magnitude pre-`sqrtf` `var_f20` accumulation and only `pad5` retained
- `./diff.sh -o func_80049794 -s --compress-matching 4 --no-pager` -> one-pad `pad5` variant also introduces target `$f20/$f21` saves but keeps frame widened to `0x100` and worsens the focused object score to `CURRENT (4125)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and early zero in `$f16` instead of target `$f14`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with the first speed-magnitude sum routed through `spEC` before `sqrtf`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `spEC = x*x + z*z; spEC += y*y; var_f20 = sqrtf(spEC) - 2.0` worsens the focused object score from baseline `CURRENT (2550)` to `CURRENT (3300)` and still does not introduce target `$f20/$f21` saves, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with grounded-wheel zeroing routed through `var_f0`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `var_f0 = 0.0f; racer->unk84 = var_f0; racer->unk88 = var_f0` produces no object change from baseline and leaves the focused score unchanged at `CURRENT (2550)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with the final axis swap routed through the now-dead `splinePos` local
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> `splinePos = diffX; diffX = diffZ; diffZ = -splinePos` worsens the focused object score from baseline `CURRENT (870)` to `CURRENT (1698)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with `gCurrentCarSteerVel = (updateRateF > 0.0f) * 0`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> initialized no-op comparison improves the focused object score from `CURRENT (2550)` to `CURRENT (2490)`, matching the older `FAKEMATCH` no-op family, but remains nonmatching and still lacks target `$f20/$f21` saves
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles in the current checkout
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and early zero still allocated as `$f16` instead of target `$f14`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with grounded-wheel zeroing rewritten as `racer->unk88 = (racer->unk84 = 0.0f)`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> chained early-zero assignment produces no object change from baseline and leaves the focused score unchanged at `CURRENT (2550)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with the final axis swap routed through the now-dead `scale` local
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> `scale = diffX; diffX = diffZ; diffZ = -scale` worsens the focused object score from baseline `CURRENT (870)` to `CURRENT (1698)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles in the current checkout
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and early zero still allocated as `$f16` instead of target `$f14`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with `f64 var_f14`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `f64 var_f14` widens the frame to `0x100`, worsens the focused object score to `CURRENT (10933)`, and still does not introduce target `$f20/$f21` prologue saves
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `python3 tools/query_goal_state.py next --compact --refresh` -> recommends `func_80049794`; 4 default candidates, 3 exhausted notes skipped
- `python3 tools/check_active_surface.py` -> active surface ok
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles in the current checkout
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and early zero still allocated as `$f16` instead of target `$f14`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with the interpolated misc-asset speed factor carried through `segmentZVelocity` into the later top-speed, boost, throttle, and brake uses
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `segmentZVelocity` carry-through worsens the focused object score to `CURRENT (2800)`, moves the key speed/gravity float family from `$f14` to `$f16`, and still does not introduce target `$f20/$f21` prologue saves
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `python3 tools/query_goal_state.py next --compact --refresh` -> recommends `func_80049794`; 4 default candidates, 3 exhausted notes skipped
- `python3 tools/check_active_surface.py` -> active surface ok
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` baseline C candidate compiles in the current checkout
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (870)` in the final lateral/vertical offset block
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with `tempY = obj->trans.y_position - tempY; diffY = tempY / divisor`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> final vertical `tempY` numerator probe worsens the focused object score to `CURRENT (1650)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `python3 tools/query_goal_state.py next --compact --refresh` -> recommends `func_80049794`; 4 default candidates, 3 exhausted notes skipped
- `python3 tools/check_active_surface.py` -> active surface ok
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` baseline C candidate compiles in the current checkout
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (870)` in the final lateral/vertical offset block
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with `distance = diffX; diffX = diffZ; diffZ = -distance`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> `distance` axis-swap temporary worsens the focused object score to `CURRENT (1548)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles in the current checkout
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and early zero still allocated as `$f16` instead of target `$f14`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with `f32 var_f14 = 0.0f` at declaration
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> declaration-initialized `var_f14` leaves the focused object score unchanged at `CURRENT (2550)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` baseline C candidate compiles in the current checkout
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (870)` in the final lateral/vertical offset block
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with final vertical numerator routed through `pad3`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> final vertical `pad3` numerator probe worsens the focused object score to `CURRENT (1680)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles in the current checkout
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and broad `$f14`/`$f20` allocation drift
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with a dedicated early `f32 zero` local for grounded-wheel zeroing and the early `racerVelocity` clamp
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> early `zero` local widens the frame to `0x100`, worsens the focused object score to `CURRENT (3035)`, and still does not introduce target `$f20/$f21` saves, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with inverse gravity rewritten as `var_f20 = 1.0 - (var_f20 * 0.25)`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> inverse-gravity `0.25` multiplication spelling leaves the focused object score unchanged at `CURRENT (2550)` and still does not introduce target `$f20/$f21` saves, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with a `register f64 speedMagnitude` staging local before `var_f20 = speedMagnitude - 2.0`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `register f64 speedMagnitude` widens the frame to `0x100`, worsens the focused object score to `CURRENT (3007)`, and still does not introduce target `$f20/$f21` prologue saves, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `python3 tools/query_goal_state.py next --compact --refresh` -> recommends `func_80049794`; 4 default candidates, 3 exhausted notes skipped
- `python3 tools/check_active_surface.py` -> active surface ok
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles in the current checkout
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and broad `$f14`/`$f20` allocation drift
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with the first speed-derived upper clamp written as `4.0f` compare and assignment
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> upper-clamp `4.0f` literal probe worsens the focused object score to `CURRENT (4975)` and still does not introduce target `$f20/$f21` prologue saves, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `git fetch upstream` -> fetched upstream refs; `upstream/master` is
  `d940feda` and does not contain a newer `func_80049794` source match
- `python3 tools/query_goal_state.py next --compact --refresh` -> recommends `func_80049794`; 4 default candidates, 3 exhausted notes skipped
- `python3 tools/check_active_surface.py` -> active surface ok
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles in the current checkout
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and broad `$f14`/`$f20` allocation drift
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with boss adjustment rewritten as `var_f20 = (var_f20 * 0.5) - 1.0`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> boss-adjustment algebra rewrite worsens the focused object score to `CURRENT (3520)` and still does not introduce target `$f20/$f21` prologue saves, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `python3 tools/check_active_surface.py` -> active surface ok
- `python3 tools/query_goal_state.py next --compact --refresh` -> recommends `func_80049794`; 4 default candidates, 3 exhausted notes skipped
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles in the current checkout
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and broad `$f14`/`$f20` allocation drift
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with a separate `f64 speedMagnitude` local before `var_f20 = speedMagnitude - 2.0`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `f64 speedMagnitude` widens the frame to `0x108`, worsens the focused object score to `CURRENT (3163)`, and still does not introduce target `$f20/$f21` prologue saves, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` baseline C candidate compiles
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> baseline promoted `func_80059208` remains `CURRENT (870)`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with `pad = splinePos * diffX; pad2 = -checkpointDot; distance = obj->trans.z_position; pad += diffZ * distance`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> partial-object-dot split produces no object change from baseline and leaves the focused object score unchanged at `CURRENT (870)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles in the current checkout
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted baseline remains `CURRENT (2550)` with missing target `$f20/$f21` saves and broad `$f14`/`$f20` allocation drift
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with the shared `var_f20` local changed directly to `f64`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> direct `f64 var_f20` widens the frame to `0x108`, worsens the focused object score to `CURRENT (10417)`, and still does not introduce target `$f20/$f21` prologue saves, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with `volatile f32 var_f20`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `volatile f32 var_f20` worsens the focused object score to `CURRENT (6441)` through broad stack traffic and still does not introduce target `$f20/$f21` prologue saves, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` baseline C candidate compiles
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> baseline promoted `func_80059208` remains `CURRENT (870)`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with `register f32 tempX` and `register f32 tempZ`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> `register f32 tempX`/`tempZ` produces no object change from baseline and leaves the focused object score unchanged at `CURRENT (870)`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with `volatile f32 pad`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> real `volatile f32 pad` final-block probe worsens the focused object score to `CURRENT (1695)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` existing C candidate compiles in the current checkout
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted existing C remains nonmatching at focused object `CURRENT (2550)` with the same missing target `$f20/$f21` save/allocation split
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with a `register f32 gravity` local from the first speed magnitude through `obj->y_velocity -= gravity`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `register f32 gravity` widens the frame to `0x100`, worsens the focused object score to `CURRENT (2959)`, and still does not introduce target `$f20/$f21` saves
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with explicit `racerMiscAssetIdx = (s32) var_f14`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> explicit misc-asset index cast leaves the focused object score unchanged at `CURRENT (2550)` and still does not introduce target `$f20/$f21` saves
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with default constant setup reordered to `spD0 = 0.02; spD4 = 0.01; spD8 = 0.004`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> default `spD0` before `spD4` constant order leaves the focused object score unchanged at `CURRENT (2550)` and still does not introduce target `$f20/$f21` saves
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` existing C candidate compiles
- `./diff.sh func_80049794` immediately after object-only rebuild printed stale `CURRENT (0)`; object disassembly still lacked target `$f20/$f21` saves, so the stale focused output was not accepted
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` with promoted baseline `func_80049794` -> verify fails as expected for the nonmatching source, CRC calculated `0x5FDDE03F/0xEF7A0514`
- `./diff.sh func_80049794` after relink -> promoted baseline remains linked `CURRENT (2760)` in this checkout and still lacks target `$f20/$f21` prologue saves
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with `var_f14 = racer->velocity` loaded before the first `sqrtf` block and the later duplicate load removed
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` with the early `var_f14` lifetime probe -> verify fails as expected for the nonmatching source, CRC calculated `0xE934957B/0xA22C402C`
- `./diff.sh func_80049794` after relink -> early `var_f14` lifetime probe worsens to `CURRENT (5000)`, moves the absolute-velocity path into `$f16`, and still does not add target `$f20/$f21` saves
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with inverse gravity staged through `var_f2`
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` with the inverse `var_f2` probe -> verify fails as expected for the nonmatching source, CRC calculated `0xAC28F349/0xE479ED58`
- `./diff.sh func_80049794` after relink -> `var_f2 = var_f20 / 4.0; var_f20 = 1.0 - var_f2` worsens to `CURRENT (3850)` and still does not add target `$f20/$f21` saves
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/tracks.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `trackbg_render_flashy` baseline C candidate compiles
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` with promoted baseline `trackbg_render_flashy` -> verify fails as expected for the nonmatching source, CRC calculated `0x93D338FF/0x03D9C8FE`
- `./diff.sh trackbg_render_flashy --no-pager` after relink -> promoted baseline remains linked `CURRENT (1808)`, with first drift in early position-array float-register allocation (`$f18` target versus `$f16` current)
- `gmake build/src/tracks.c.o CROSS=tools/binutils/mips64-elf-` -> swapped `scaledXSin`/`scaledXCos` declaration order, `register f32 scaledXSin`, and `register f32 scaledXCos` probes all compile but produce no object change from the promoted baseline
- `gmake build/src/tracks.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `trackbg_render_flashy` C candidate compiles with `zPositions[3] = scaledXCos + (xSin * 1280.0f)`
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` with the `zPositions[3]` multiply spelling -> verify fails as expected for the nonmatching probe, CRC calculated `0xF82B92BE/0x5DCC04AE`
- `./diff.sh trackbg_render_flashy --no-pager` after relink -> `zPositions[3] = scaledXCos + (xSin * 1280.0f)` worsens to `CURRENT (5579)` by inserting `swc1 $f0, 0x110(sp)` and shifting later scheduling/global offsets
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with final `pad3 = 5.0f` lateral clamp limits
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> final `pad3 = 5.0f` lateral clamp-local probe worsens the focused object score to `CURRENT (1015)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` baseline C candidate compiles
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> baseline promoted `func_80059208` remains `CURRENT (870)`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with `pad2 = -checkpointDot; pad = pad2 + objectDot; diffX = -(pad / divisor)`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> final lateral-correction sum-dataflow probe leaves the focused object score unchanged at `CURRENT (870)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with the first `var_f20` clamp written as `0.0f` comparisons/assignments
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> first `var_f20` zero-clamp `0.0f` literal probe worsens the focused object score to `CURRENT (3355)` and still does not introduce target `$f20/$f21` prologue saves, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted existing C remains nonmatching at focused object `CURRENT (2550)` with the same missing target `$f20/$f21` save/allocation split
- `./diff.sh func_80049794 -s --compress-matching 3 --no-pager` after object-only rebuild printed a misleading stale `CURRENT (0)`; do not accept this function without relink/full gate evidence
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with an empty `if (var_f20) {}` lifetime no-op immediately after `apply_vehicle_rotation_offset`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> post-call `var_f20` lifetime no-op worsens the focused object score to `CURRENT (3045)` and still does not introduce target `$f20/$f21` prologue saves, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/tracks.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_8002B0F4` C candidate compiles
- `./diff.sh func_8002B0F4 -s --compress-matching 3 --no-pager` immediately after object rebuild printed a misleading stale `CURRENT (0)`; do not accept this function from object-only/stale focused output
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` with promoted baseline `func_8002B0F4` -> verify failed as expected for the real nonmatching source, CRC calculated `0x7856718A/0x66208CAA`
- `./diff.sh func_8002B0F4 -s --no-pager` after relink -> promoted baseline remains linked `CURRENT (2780)` with first drift at `gCurrentLevelModel` hoisting before the outer segment loop
- `gmake build/src/tracks.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_8002B0F4` C candidate compiles with `XInInt`/`ZInInt` assigned before `get_inside_segment_count_xz` and used for the call
- `./diff.sh func_8002B0F4 -s --compress-matching 3 --no-pager` -> explicit pre-call integer locals leave the linked score unchanged at `CURRENT (2780)`, nonmatching
- `gmake build/src/tracks.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_8002B0F4` C candidate compiles with a local `LevelModel *levelModel` loaded through a volatile pointer cast at the segment and texture access sites
- `./diff.sh func_8002B0F4 -s --compress-matching 3 --no-pager` -> volatile local level-model reloads leave the linked score unchanged at `CURRENT (2780)`, nonmatching
- `python3 tools/check_active_surface.py` after restoring guarded matching source -> active surface ok
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `python3 tools/query_goal_state.py next --compact --refresh` -> recommends `func_80049794`; 4 default candidates, 3 exhausted notes skipped
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with `diffY = diffX; diffX = diffZ; diffY = -diffY; diffZ = diffY`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> staged axis negation worsens the focused object score to `CURRENT (974)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with `racerTrickType = racer->trickType` staged before the course-height expression
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> staged `racerTrickType` local leaves the focused object score unchanged at `CURRENT (2550)` and still does not introduce target `$f20/$f21` saves
- `python3 tools/check_active_surface.py` after restoring guarded matching source -> active surface ok
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` baseline C candidate compiles
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted existing C remains nonmatching at focused object `CURRENT (2550)` with the same missing target `$f20/$f21` save/allocation split
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` baseline C candidate compiles
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> baseline promoted `func_80059208` remains `CURRENT (870)`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with positive checkpoint dot folded into `pad` via a separate `pad2`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> positive `pad2` plus `pad = objectDot - pad2; diffX = -(pad / divisor)` leaves the focused object score unchanged at `CURRENT (870)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with the positive checkpoint dot subtracted directly from `pad`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> `pad = objectDot; pad -= checkpointDot; diffX = -(pad / divisor)` leaves the focused object score unchanged at `CURRENT (870)`, nonmatching
- `python3 tools/check_active_surface.py` after restoring guarded matching source -> active surface ok
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` baseline C candidate compiles
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> baseline promoted `func_80059208` remains `CURRENT (870)`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with the final lateral dot product rewritten as a relative-position expression
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> relative-position lateral dot-product rewrite worsens the focused object score to `CURRENT (1897)`, nonmatching
- `python3 tools/check_active_surface.py` after restoring guarded matching source -> active surface ok
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with a dedicated `f32 gravity` local for the early gravity block through `apply_vehicle_rotation_offset`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> dedicated `gravity` local widens the frame to `0x100` and worsens the focused object score to `CURRENT (2959)`, nonmatching
- `python3 tools/check_active_surface.py` after restoring guarded matching source -> active surface ok
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with inverse gravity rewritten as `(4.0 - var_f20) / 4.0`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `(4.0 - var_f20) / 4.0` worsens the focused object score to `CURRENT (4250)`, nonmatching
- `python3 tools/check_active_surface.py` after restoring guarded matching source -> active surface ok
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with `spA3 = FALSE` moved after the inverse-gravity assignment and before the course-height subtraction
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `spA3` after the inverse-gravity assignment worsens the focused object score to `CURRENT (2790)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with the course-height expression split into two assignments
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `var_f2 = gCurrentCourseHeight - 50.0; var_f2 -= obj->trans.y_position;` worsens the focused object score to `CURRENT (3530)`, nonmatching
- `python3 tools/check_active_surface.py` after restoring guarded matching source -> active surface ok
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with the early wave velocity magnitude routed through `var_f20`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> reusing `var_f20` instead of the separate `racerVelocity` local worsens the focused object score to `CURRENT (2704)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with `MtxF sp60`, with pointer-type warnings at the `mtxf_transform_point` calls
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `MtxF sp60` widens the frame to `0x128` and worsens the focused object score to `CURRENT (4567)`, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` after restoring guarded matching source -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with the final `pad + pad2` sum routed through `pad3`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> final-sum `pad3` routing leaves the focused object score unchanged at `CURRENT (870)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with `volatile f32 pad2`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> volatile `pad2` worsens the focused object score to `CURRENT (955)` by forcing extra stack store/load traffic and shifting final-block scheduling, nonmatching
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` -> `Verify: OK`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` existing C candidate compiles
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> promoted existing C remains nonmatching at object `CURRENT (2550)` with the same missing target `$f20/$f21` save/allocation split
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` with `func_80049794` promoted -> verify fails as expected for the nonmatching candidate, CRC calculated `0x5FDDE03F/0xEF7A0514`
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with `register f32 var_f20` plus `register f32 racerVelocity`
- `./diff.sh func_80049794 -s --compress-matching 3 --no-pager` -> promoted C remains nonmatching at linked `CURRENT (2760)`; the combined `register f32 var_f20` / `register f32 racerVelocity` probe leaves the score unchanged and still does not introduce target `$f20/$f21` prologue saves
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with the first speed magnitude split through `var_f2`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `var_f2 = sqrtf(...) - 2.0; var_f20 = var_f2;` plus using `var_f2` for the boss adjustment leaves the focused object score unchanged at `CURRENT (2550)` and still does not introduce target `$f20/$f21` prologue saves
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with `register f32 var_f2`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> `register f32 var_f2` leaves the focused object score unchanged at `CURRENT (2550)` and still does not introduce target `$f20/$f21` prologue saves
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with buoyancy `var_f20 = -1.0f` materialized before `gCurrentStickY = -60`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> splitting the buoyancy `-1.0f` materialization matches that local target ordering direction but worsens the focused object score to `CURRENT (2590)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with the empty `if ((!racerSteerAngle)) {}` lifetime hint removed from the wave drift branch
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> removing the empty `racerSteerAngle` lifetime hint leaves the focused object score unchanged at `CURRENT (2550)` and still does not move the `$f20/$f21` prologue-save split
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with the top-speed multiply split into `var_f14 *= handle_racer_top_speed(...); var_f14 *= 1.8;`
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> splitting the top-speed multiply widens the frame to `0x100` and worsens the focused object score to `CURRENT (5977)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80049794` C candidate compiles with `f32 var_f20 = 0.0f` initialized at declaration
- `./diff.sh -o func_80049794 -s --compress-matching 3 --no-pager` -> initializing `var_f20` at declaration leaves the focused object score unchanged at `CURRENT (2550)` and still does not introduce target `$f20/$f21` prologue saves
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate still compiles for baseline comparison
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> baseline promoted `func_80059208` remains `CURRENT (870)` with the same final-offset expression/load-order drift; no rejected final-block probe was repeated
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> reusing the now-dead `scale` local for the final `5.0f` lateral clamp worsens the focused object score from baseline `CURRENT (870)` to `CURRENT (1015)`, nonmatching
- `./diff.sh func_80059208 -s --compress-matching 3 --no-pager` -> moving `pad`/`pad2` into a nested final lateral-offset block worsens to `CURRENT (1336)` with 8-byte stack-slot drift; accumulating into `pad2` with `pad2 += pad; diffX = -(pad2 / divisor)` leaves the linked score unchanged at `CURRENT (870)`, nonmatching
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> baseline promoted C remains `CURRENT (870)`; routing the axis swap through `pad3` worsens to `CURRENT (1698)`, and computing the negated rotated axis first through `diffY` worsens to `CURRENT (1090)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with final `diffX`/`diffY` clamps spelled through `CLAMP`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> final `CLAMP(diffX, -5.0f, 5.0f)` / `CLAMP(diffY, -100.0f, 100.0f)` probe leaves the focused object score unchanged at `CURRENT (870)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with only `obj->trans.z_position` inlined in the final object dot product
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> keeping `splinePos = obj->trans.x_position` local while inlining only `obj->trans.z_position` worsens the focused object score to `CURRENT (1684)`, nonmatching
- `gmake build/src/racer.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_80059208` C candidate compiles with the final object dot routed through `pad3`
- `./diff.sh -o func_80059208 -s --compress-matching 3 --no-pager` -> `pad3 = objDot; diffX = -((pad3 + pad2) / divisor)` leaves the focused object score unchanged at `CURRENT (870)`, nonmatching
- `gmake build/src/tracks.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `trackbg_render_flashy` C candidate compiles
- `./diff.sh trackbg_render_flashy -s --compress-matching 3 --no-pager` -> baseline promoted C remains `CURRENT (1808)`; flipping only `xPositions[2]` to `(xSin * 1280.0f) + scaledXCos` leaves the score unchanged at `CURRENT (1808)`, and replacing only `xPositions[2]` with `scaledXCos + scaledXSin` worsens to `CURRENT (12021)` with frame `0x150`, nonmatching
- `gmake build/src/tracks.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `trackbg_render_flashy` outer-ring additive-double and `negScaledXCos` C candidates compile
- `./diff.sh trackbg_render_flashy --no-pager` -> outer-ring additive-double probe remains `CURRENT (1808)`, with the same early `f18` versus `f16`/store-scheduling drift
- `gmake -j4 CROSS=tools/binutils/mips64-elf-` with promoted/probed `trackbg_render_flashy` -> verify fails as expected for the nonmatching candidate, CRC calculated `0x93D338FF/0x03D9C8FE`
- `./diff.sh trackbg_render_flashy --no-pager` -> adding a named `negScaledXCos` temporary for first/outer position expressions also remains `CURRENT (1808)`, nonmatching
- `gmake build/src/tracks.c.o CROSS=tools/binutils/mips64-elf-` -> promoted `func_8002B0F4` C candidate compiles
- `./diff.sh func_8002B0F4 -s --compress-matching 3 --no-pager` -> baseline promoted C scores `CURRENT (2780)` with first drift around `gCurrentLevelModel` hoisting before the segment loop; inserting `if (gCurrentLevelModel) {}` worsens to `CURRENT (6347)`, and swapping the segment/bounding-box pointer setup order worsens to `CURRENT (3885)`, nonmatching
- Prior closeout validation retained below for continuity; current source was restored to guarded matching mode before the final `Verify: OK`.

## Blockers Or Unknowns

- Plain `gmake -j4` still selects Homebrew `mips-linux-gnu-ld`; keep using `CROSS=tools/binutils/mips64-elf-`.
- `func_80049794` remains active. Target saves `$f21/$f20` at `0x20/0x24(sp)` and uses `$f20` broadly for plane physics float temporaries; current promoted C keeps those values in caller-scratch float registers and does not save `$f20/$f21`.
- Rejected `func_80049794` probes: `register f32 var_f20`, moving `var_f20` declaration, explicit `f64 var_f20_d`, inline clamp casts, `/ 2.0` and `/ 4.0` multiply rewrites, splitting the initial `sqrtf` result into a named local, two-step first `sqrtf` subtraction, post-`sqrtf` `var_f2` dataflow (`var_f2 = sqrtf(...) - 2.0; var_f20 = var_f2;`), `register f32 var_f2`, removing the empty `if ((!racerSteerAngle)) {}` lifetime hint, splitting the top-speed multiply into two assignments, `register f32 var_f14`, moving `var_f14` into the early local group, `register f32 segmentZVelocity`, direct assignment of the misc-asset interpolation into `var_f14`, delaying the `segmentZVelocity` copy until `handle_racer_top_speed`, reusing `racerMiscAssetIdx` for the `var_f14` fractional calculation, reordering the misc-asset interpolation to current-then-next expression order, deriving `var_f0` from the already loaded `var_f14` velocity, explicit `4.0f`/`3.0f` velocity clamp constants, explicit `-1.0f`/`10.0f` buoyancy constants, splitting buoyancy so `var_f20 = -1.0f` materializes before `gCurrentStickY = -60`, starting `var_f20` lifetime at the first grounded-wheel zeroing, `register` hints on `spEC`/`spD8`/`spD4`/`spD0`, naming `gRacerWaveCount - 1` in `var_v1` before the wave scan, explicitly materializing the early zero through `var_f14` before the grounded wheel reset, moving `spA3 = FALSE` after the inverse-gravity calculation, combining `register f32 var_f20` with `register f32 var_f14`, combining `register f32 var_f20` with `register f32 racerVelocity`, and routing the early grounded-wheel zero through `spCC`. The wave-scan probe matched the intended `v1/a0` idea locally but worsened the focused score to `CURRENT (5445)` by increasing register pressure and spilling/reshaping `spA2`; the early-zero `var_f14`, `spA3` scheduling, combined register, `spCC` zero, two-step first `sqrtf` subtraction, post-`sqrtf` `var_f2` dataflow, `register f32 var_f2`, empty-`racerSteerAngle` hint removal, `racerMiscAssetIdx` fractional-reuse, explicit buoyancy-constant, and early-`var_f20` lifetime probes compiled but left the score unchanged at `CURRENT (2550)` and did not move the `$f14/$f16` / `$f20` allocation split. The interpolation-order probe worsened to `CURRENT (2585)`, the single-load velocity probe worsened to `CURRENT (3095)`, the explicit float clamp constants worsened to `CURRENT (3045)`, the split buoyancy materialization probe worsened to `CURRENT (2590)`, and the split top-speed multiply probe widened the frame to `0x100` and worsened to `CURRENT (5977)`. The `register f32 var_f20` plus `register f32 racerVelocity` combination compiled but left the current linked score unchanged at `CURRENT (2760)` and still did not add the target `$f20/$f21` saves. Do not repeat those exact source shapes.
- Additional `func_80049794` rejected probes from recent sessions: initializing `var_f20` at declaration (`f32 var_f20 = 0.0f`) compiled but left the focused object score unchanged at `CURRENT (2550)` and still did not add the target `$f20/$f21` saves; reusing `var_f20` for the early wave velocity magnitude instead of the separate `racerVelocity` local worsened to `CURRENT (2704)`; changing `sp60` from `f32 sp60[4]` to `MtxF sp60` widened the frame to `0x128`, emitted pointer-type warnings, and worsened to `CURRENT (4567)`. Do not repeat those source shapes.
- Additional `func_80049794` course-height/`spA3` rejected probes: moving `spA3 = FALSE` after the inverse-gravity assignment and before the course-height subtraction worsened the focused object score to `CURRENT (2790)` in this checkout; splitting the course-height expression into `var_f2 = gCurrentCourseHeight - 50.0; var_f2 -= obj->trans.y_position;` worsened the focused object score to `CURRENT (3530)`. Do not repeat those source shapes.
- Additional `func_80049794` inverse-gravity rejected probe: rewriting `var_f20 = 1.0 - (var_f20 / 4.0)` as `var_f20 = (4.0 - var_f20) / 4.0` compiled but worsened the focused object score to `CURRENT (4250)`. Do not repeat this source shape.
- Additional `func_80049794` lifetime rejected probe: splitting the early gravity value into a dedicated `f32 gravity` local from the initial speed magnitude through the `obj->y_velocity -= gravity` store compiled but widened the frame to `0x100` and worsened the focused object score to `CURRENT (2959)`. Do not repeat this source shape.
- Additional `func_80049794` trick-type scheduling rejected probe: staging `racerTrickType = racer->trickType` before the course-height expression and using that local in the `trickType` range test compiled but left the focused object score unchanged at `CURRENT (2550)` and still did not introduce target `$f20/$f21` saves. Do not repeat this source shape.
- Additional `func_80049794` post-call lifetime rejected probe: adding an empty `if (var_f20) {}` immediately after `apply_vehicle_rotation_offset` compiled but worsened the focused object score to `CURRENT (3045)` and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` zero-clamp literal rejected probe: rewriting the first speed-derived `var_f20` zero clamp as `0.0f` comparisons/assignments compiled but worsened the focused object score to `CURRENT (3355)` and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` early `var_f14` lifetime rejected probe: loading `var_f14 = racer->velocity` before the first `sqrtf` block and removing the later duplicate load compiled but worsened the relinked focused score to `CURRENT (5000)`, moved the absolute-velocity path into `$f16`, and still did not introduce target `$f20/$f21` saves. Do not repeat this source shape.
- Additional `func_80049794` inverse-fraction rejected probe: staging `var_f2 = var_f20 / 4.0; var_f20 = 1.0 - var_f2` compiled but worsened the relinked focused score to `CURRENT (3850)` and still did not introduce target `$f20/$f21` saves. Do not repeat this source shape.
- Additional `func_80049794` misc-asset index cast rejected probe: explicitly casting the assignment as `racerMiscAssetIdx = (s32) var_f14` compiled but left the focused object score unchanged at `CURRENT (2550)` and still did not introduce target `$f20/$f21` saves. Do not repeat this source shape.
- Additional `func_80049794` default-constant order rejected probe: assigning `spD0 = 0.02` before `spD4 = 0.01` compiled but left the focused object score unchanged at `CURRENT (2550)` and still did not introduce target `$f20/$f21` saves. Do not repeat this source shape.
- Additional `func_80049794` register-gravity rejected probe: splitting the early gravity path into a `register f32 gravity` local from the first speed magnitude through `obj->y_velocity -= gravity` compiled but widened the frame to `0x100`, worsened the focused object score to `CURRENT (2959)`, and still did not introduce target `$f20/$f21` saves. Do not repeat this source shape.
- Additional `func_80049794` volatile-lifetime rejected probe: making the shared `var_f20` local volatile compiled but worsened the focused object score to `CURRENT (6441)` through broad stack traffic and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` direct-double rejected probe: changing the shared `var_f20` local itself to `f64` compiled but widened the frame to `0x108`, worsened the focused object score to `CURRENT (10417)`, and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` speed-magnitude double-local rejected probe: splitting the first speed magnitude through a separate `f64 speedMagnitude` local before assigning `var_f20 = speedMagnitude - 2.0` compiled but widened the frame to `0x108`, worsened the focused object score to `CURRENT (3163)`, and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` register speed-magnitude double-local rejected probe: splitting the first speed magnitude through a `register f64 speedMagnitude` local before assigning `var_f20 = speedMagnitude - 2.0` compiled but widened the frame to `0x100`, worsened the focused object score to `CURRENT (3007)`, and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` boss-adjustment algebra rejected probe: rewriting `var_f20 = ((var_f20 - 2.0) / 2.0)` as `var_f20 = (var_f20 * 0.5) - 1.0` compiled but worsened the focused object score to `CURRENT (3520)` and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` upper-clamp literal rejected probe: rewriting the first speed-derived upper clamp as explicit single-precision (`if (var_f20 > 4.0f) { var_f20 = 4.0f; }`) compiled but worsened the focused object score to `CURRENT (4975)` and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` inverse-gravity multiplication rejected probe: rewriting `var_f20 = 1.0 - (var_f20 / 4.0)` as `var_f20 = 1.0 - (var_f20 * 0.25)` compiled but left the focused object score unchanged at `CURRENT (2550)` and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` in-place inverse-gravity rejected probe: rewriting `var_f20 = 1.0 - (var_f20 / 4.0)` as `var_f20 /= 4.0; var_f20 = 1.0 - var_f20` compiled but worsened the focused object score to `CURRENT (3435)` and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` course-height grouping rejected probe: rewriting `(gCurrentCourseHeight - 50.0) - obj->trans.y_position` as `var_f2 = gCurrentCourseHeight - obj->trans.y_position; var_f2 -= 50.0` compiled but worsened the focused object score to `CURRENT (3755)` and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` early-zero local rejected probe: introducing a dedicated `f32 zero` local for the grounded-wheel zeroing and early `racerVelocity` clamp compiled but widened the frame to `0x100`, worsened the focused object score to `CURRENT (3035)`, and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` `var_f14` declaration-initializer rejected probe: initializing `var_f14` at declaration (`f32 var_f14 = 0.0f`) compiled but left the focused object score unchanged at `CURRENT (2550)`, kept the early zero in `$f16`, and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` segmentZVelocity carry-through rejected probe: carrying the interpolated misc-asset speed factor through `segmentZVelocity` into the later `handle_racer_top_speed`, boost override, throttle, and brake uses compiled but worsened the focused object score to `CURRENT (2800)`; it moved the key speed/gravity float family from `$f14` to `$f16` and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` `f64 var_f14` rejected probe: changing the long-lived misc-speed/top-speed carrier `var_f14` from `f32` to `f64` compiled but widened the frame to `0x100`, worsened the focused object score to `CURRENT (10933)`, and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` chained early-zero rejected probe: rewriting grounded-wheel zeroing as `racer->unk88 = (racer->unk84 = 0.0f)` compiled but produced no object change from the promoted baseline, left the focused object score unchanged at `CURRENT (2550)`, and still kept the early zero in `$f16` instead of target `$f14`. Do not repeat this source shape.
- Additional `func_80049794` initialized no-op steer-vel rejected probe: replacing `gCurrentCarSteerVel = 0` with `gCurrentCarSteerVel = (updateRateF > 0.0f) * 0` compiled and improved the focused object score to `CURRENT (2490)`, matching the older `FAKEMATCH` no-op family, but remained nonmatching and still did not introduce target `$f20/$f21` saves. Do not accept or repeat this source shape without a real exact-match path.
- Additional `func_80049794` pre-sqrt accumulation rejected probe: splitting the first speed magnitude into `var_f20 = (obj->x_velocity * obj->x_velocity) + (obj->z_velocity * obj->z_velocity); var_f20 += obj->y_velocity * obj->y_velocity; var_f20 = sqrtf(var_f20) - 2.0` introduced the target `$f20/$f21` prologue saves but widened the frame to `0x100` and worsened the focused object score to `CURRENT (4049)`. Removing the two leading unused `pad5`/`pad7` locals with that same pre-`sqrtf` accumulation restored the target `0xf8` frame and kept `$f20/$f21` saves, but still worsened the focused object score to `CURRENT (3932)` by shifting local stack slots and later scheduling. Do not repeat either source shape.
- Additional `func_80049794` `spEC` pre-sqrt sum rejected probe: routing the first speed-magnitude sum through the existing `spEC` local (`spEC = x*x + z*z; spEC += y*y; var_f20 = sqrtf(spEC) - 2.0`) compiled but worsened the focused object score from baseline `CURRENT (2550)` to `CURRENT (3300)` and still did not introduce target `$f20/$f21` prologue saves. Do not repeat this source shape.
- Additional `func_80049794` one-pad pre-sqrt accumulation rejected probe: retaining only one of the two leading unused pads (`pad7` only or `pad5` only) with `var_f20 = x*x + z*z; var_f20 += y*y; var_f20 = sqrtf(var_f20) - 2.0` compiled and still introduced target `$f20/$f21` saves, but both variants kept the frame widened to `0x100` and worsened the focused object score to `CURRENT (4125)`. Do not repeat this source shape.
- Additional `func_80049794` trailing-pad pre-sqrt accumulation rejected/progress probe: removing both trailing unused pads (`pad3`/`pad4`) with the same pre-`sqrtf` `var_f20` accumulation kept the target `0xf8` frame and target `$f20/$f21` saves, improving to `CURRENT (3620)`, but still left broad zero/wave/scheduling drift and did not match. Retaining exactly one trailing pad (`pad4` only or `pad3` only) widened the frame back to `0x100` and regressed to `CURRENT (4049)` in both cases. Do not repeat simple trailing-pad toggles; if continuing this family, inspect the remaining local-slot/scheduling drift from the both-trailing-pads-removed candidate.
- Additional `func_80049794` combined leading/trailing pad pre-sqrt accumulation rejected probe: removing both leading unused pads (`pad5`/`pad7`) and both trailing unused pads (`pad3`/`pad4`) with the same pre-`sqrtf` `var_f20` accumulation retained target `$f20/$f21` saves but shrank the frame to `0xf0` and scored `CURRENT (3737)`, worse than the both-trailing-pads-only candidate. Do not repeat this source shape.
- Additional `func_80049794` combined-pad declaration rejected probe: changing the leading unused scalars to `UNUSED s32 pad[2]` compiled but produced no object change from the promoted baseline and left the focused object score unchanged at `CURRENT (2550)`. Do not repeat this source shape.
- Additional `func_80049794` double-literal grounded-zero rejected probe: spelling the first grounded-wheel zero stores as `racer->unk84 = 0.0; racer->unk88 = 0.0` compiled and changed the early zero allocation shape, but worsened the focused object score to `CURRENT (4685)` and still did not introduce target `$f20/$f21` saves. Do not repeat this source shape.
- Additional `func_80049794` integer-literal grounded-zero rejected probe: spelling the first grounded-wheel zero stores as `racer->unk84 = 0; racer->unk88 = 0` compiled but also worsened the focused object score to `CURRENT (4685)`, splitting the stores across `$f4`/`$f6` instead of target `$f14`. Do not repeat this source shape.
- The `FAKEMATCH` no-op around `gCurrentCarSteerVel` can improve the focused score from `2550` to `2490`, but it is still nonmatching and should not be accepted as progress without an exact-match path.
- `func_80059208` is close and should stay active. Existing C promotes to `CURRENT (870)`; target keeps the negated `pad2` temporary and adds it to `pad`, while current folds the expression into a subtract. Rejected probes: `pad`/`pad2` reorder, `pad += pad2`, `register f32 pad2`, signed-zero negation, removing `UNUSED`, two-step negation, operand-order swaps, inline `pad2`, split final assignment, delayed `z_position` load, `register f32 pad`, empty `if (1) {}` barrier near `pad2`, transient `distance` holder, `register f32 distance`, `pad - (-pad2)`, target-dataflow order that computes the checkpoint dot product first, object dot product second, then negates the checkpoint term, positive `pad2` with `diffX = -((pad - pad2) / divisor)`, inlining object position loads in the dot product, inlining only `obj->trans.x_position` while retaining the `distance` z temporary, inlining only `obj->trans.z_position` while retaining the `splinePos` x temporary, routing the checkpoint dot through `pad3` before negating into `pad2`, routing the final object dot through `pad3`, routing the final `pad + pad2` sum through `pad3`, making `pad2` volatile, reusing the dead `diffY` local for the checkpoint dot, positive `pad2` with `diffX = (pad2 - pad) / divisor`, routing the axis-rotation swap through `pad3`, computing the negated rotated axis first through `diffY`, moving `pad`/`pad2` into a nested final lateral-offset block, accumulating the final sum into `pad2`, reusing the now-dead `scale` local for the final `5.0f` lateral clamp, routing the final `5.0f` lateral clamp through `pad3`, replacing the final manual `diffX`/`diffY` clamps with `CLAMP`, rewriting the final lateral correction as a relative-position dot product (`splinePos = obj->trans.x_position - tempX; distance = obj->trans.z_position - tempZ; pad = (splinePos * diffX) + (diffZ * distance); diffX = -(pad / divisor)`), folding a positive checkpoint dot through `pad2` with `pad = objectDot - pad2; diffX = -(pad / divisor)`, subtracting the positive checkpoint dot directly from `pad` before division, staging the axis negation as `diffY = diffX; diffX = diffZ; diffY = -diffY; diffZ = diffY`, and computing `pad = pad2 + objectDot; diffX = -(pad / divisor)` after the negated `pad2`. Positive `pad2`, checkpoint-dot `pad3`, object-dot `pad3`, final-sum `pad3`, positive-checkpoint-dot `pad` folding, direct `pad -= checkpointDot` folding, and `pad2 + objectDot` final-sum folding left the score unchanged at `CURRENT (870)`; volatile `pad2` worsened to `CURRENT (955)` with extra stack traffic and shifted final-block scheduling; inline object loads worsened to `CURRENT (1356)`, inline-X-only worsened to `CURRENT (1826)`, inline-Z-only worsened to `CURRENT (1684)`, checkpoint-dot `diffY` reuse worsened to `CURRENT (1465)`, positive `pad2` with direct `(pad2 - pad)` worsened to `CURRENT (1200)`, axis-swap `pad3` worsened to `CURRENT (1698)`, negated-axis-first `diffY` worsened to `CURRENT (1090)`, staged axis negation worsened to `CURRENT (974)`, nested `pad`/`pad2` worsened to `CURRENT (1336)`, `pad2 += pad; diffX = -(pad2 / divisor)` left the score unchanged at `CURRENT (870)`, the `scale = 5.0f` and `pad3 = 5.0f` clamp-local probes both worsened to `CURRENT (1015)`, the final `CLAMP` macro probe left the score unchanged at `CURRENT (870)`, and the relative-position dot-product rewrite worsened to `CURRENT (1897)`.
- Additional `func_80059208` lifetime-hint rejected probe: adding `register` to `tempX` and `tempZ` compiled but produced no object change from the promoted baseline and left the focused object score unchanged at `CURRENT (870)`. Do not repeat this source shape.
- Additional `func_80059208` volatile-pad rejected probe: making the actual final-block `pad` local volatile compiled but worsened the focused object score to `CURRENT (1695)` by forcing extra stack traffic. Do not repeat this source shape.
- Additional `func_80059208` partial-object-dot rejected probe: splitting the final object dot into `pad = splinePos * diffX`, computing the negated `pad2`, then loading `distance = obj->trans.z_position` and accumulating `pad += diffZ * distance` compiled but produced no object change from the promoted baseline and left the focused object score unchanged at `CURRENT (870)`. Do not repeat this source shape.
- Additional `func_80059208` final vertical-offset rejected probe: routing only `obj->trans.y_position - tempY` through `pad3` before `diffY = pad3 / divisor` compiled but worsened the focused object score to `CURRENT (1680)` by shifting final clamp scheduling/register allocation. Do not repeat this source shape.
- Additional `func_80059208` axis-swap rejected probe: reusing the now-dead `distance` local as the final axis-swap temporary (`distance = diffX; diffX = diffZ; diffZ = -distance`) compiled but worsened the focused object score to `CURRENT (1548)` by shifting final-offset register allocation and clamp scheduling. Do not repeat this source shape.
- Additional `func_80059208` final vertical tempY rejected probe: reusing the now-dead `tempY` local for the final vertical numerator (`tempY = obj->trans.y_position - tempY; diffY = tempY / divisor`) compiled but worsened the focused object score to `CURRENT (1650)` by shifting final vertical clamp scheduling/register allocation. Do not repeat this source shape.
- Additional `func_80059208` scale-axis-swap rejected probe: reusing the now-dead `scale` local as the final axis-swap temporary (`scale = diffX; diffX = diffZ; diffZ = -scale`) compiled but worsened the focused object score from baseline `CURRENT (870)` to `CURRENT (1698)` by reshaping the final offset register family. Do not repeat this source shape.
- Additional `func_80059208` splinePos-axis-swap rejected probe: reusing the now-dead `splinePos` local as the final axis-swap temporary (`splinePos = diffX; diffX = diffZ; diffZ = -splinePos`) compiled but worsened the focused object score from baseline `CURRENT (870)` to `CURRENT (1698)`, matching the bad `pad3`/`scale` axis-swap family. Do not repeat this source shape.
- Additional `func_80059208` numerator-negation rejected probe: moving the final lateral-correction negation into the numerator (`diffX = (-(pad + pad2)) / divisor`) compiled but worsened the focused object score from baseline `CURRENT (870)` to `CURRENT (1600)` by delaying the negation until after the folded object-dot subtraction and perturbing the final vertical temp register family. Do not repeat this source shape.
- Additional `func_80059208` final vertical diffY rejected probe: splitting the final vertical numerator through `diffY` (`diffY = obj->trans.y_position; diffY -= tempY; diffY /= divisor`) compiled but worsened the focused object score from baseline `CURRENT (870)` to `CURRENT (1242)` by extending the final vertical clamp/register drift. Do not repeat this source shape.
- Additional `func_80059208` checkpoint-dot accumulation rejected probe: splitting the checkpoint dot into `pad2 = tempZ * diffZ; pad2 += diffX * tempX; pad2 = -pad2` before the object dot compiled but left the focused object score unchanged at `CURRENT (870)` with the same final object-load/arithmetic drift. Do not repeat this source shape.
- `trackbg_render_flashy` promotes but is broader (`CURRENT (1808)`) and starts drifting in position-array setup. Rejected probes: replacing repeated first-four `(xSin * 1280.0f)` terms with `scaledXSin` widened the frame to `0x168` and worsened to `CURRENT (12121)`; reordering the index 5-8 x/z position stores to match the apparent target store sequence worsened to `CURRENT (2551)`; flipping only `xPositions[2]` to `(xSin * 1280.0f) + scaledXCos` left the score unchanged at `CURRENT (1808)`; replacing only `xPositions[2]` with `scaledXCos + scaledXSin` worsened to `CURRENT (12021)` and frame `0x150`; changing the outer-ring `2.0f * scaledXCos/scaledXSin` terms to additive doubles left the uncompressed linked score unchanged at `CURRENT (1808)` and failed promoted full verify with CRC `0x93D338FF/0x03D9C8FE`; adding a named `negScaledXCos` temporary also left the uncompressed linked score unchanged at `CURRENT (1808)`; swapping `scaledXSin`/`scaledXCos` declaration order and adding `register` hints to either local produced no object change; rewriting only `zPositions[3]` as `scaledXCos + (xSin * 1280.0f)` worsened to linked `CURRENT (5579)` by inserting `swc1 $f0, 0x110(sp)` and shifting later scheduling/global offsets, with promoted full verify CRC `0xF82B92BE/0x5DCC04AE`. Do not trust compressed-only `trackbg_render_flashy` output; use uncompressed linked diff and full verify.
- `func_8002B0F4` promotes but is broader (`CURRENT (2780)`) and starts drifting around `gCurrentLevelModel` hoisting/caching before the grid loops. Rejected probes: inserting an empty `if (gCurrentLevelModel) {}` before the segment/bounding-box pointer setup worsened to `CURRENT (6347)` and introduced broader prologue/global-offset drift; swapping the setup order to compute `currentBoundingBox` before `currentSegment` worsened to `CURRENT (3885)` while still leaving the unwanted `gCurrentLevelModel` hoist; moving `XInInt`/`ZInInt` assignment before `get_inside_segment_count_xz` and passing those locals left the linked score unchanged at `CURRENT (2780)`; loading a local `LevelModel *levelModel` through a volatile pointer cast at segment and texture access sites also left the linked score unchanged at `CURRENT (2780)`. A compressed focused diff printed stale `CURRENT (0)` before relink during this pass; confirm with a relinked focused diff and the full ROM gate before accepting this function. Keep active; do not repeat those source shapes.

## Ask The User Only If

- The retail baserom or extracted assets are missing.
- A required setup dependency cannot be installed or initialized.
- A behavior question cannot be resolved from code, symbols, or focused diff evidence.

## Next Work Packet

- Task: `DKR-MATCH-FUNC-80049794`
- Packet class: `matching_impl`
- Packet status: `active`
- Reasoning tier: `medium`
- Do not repeat this session's `func_80049794` `register f64 speedMagnitude` probe before `var_f20 = speedMagnitude - 2.0`; it widened the frame to `0x100`, worsened the focused object score to `CURRENT (3007)`, and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` first upper-clamp `4.0f` literal probe (`if (var_f20 > 4.0f) { var_f20 = 4.0f; }`); it worsened the focused object score to `CURRENT (4975)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` boss-adjustment algebra rewrite (`var_f20 = (var_f20 * 0.5) - 1.0`); it worsened the focused object score to `CURRENT (3520)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` separate `f64 speedMagnitude` probe before `var_f20 = speedMagnitude - 2.0`; it widened the frame to `0x108`, worsened the focused object score to `CURRENT (3163)`, and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` inverse-gravity `0.25` multiplication probe (`var_f20 = 1.0 - (var_f20 * 0.25)`); it left the focused object score unchanged at `CURRENT (2550)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` in-place inverse-gravity probe (`var_f20 /= 4.0; var_f20 = 1.0 - var_f20`); it worsened the focused object score to `CURRENT (3435)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` course-height grouping probe (`var_f2 = gCurrentCourseHeight - obj->trans.y_position; var_f2 -= 50.0`); it worsened the focused object score to `CURRENT (3755)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` dedicated early `f32 zero` local probe for grounded-wheel zeroing and early `racerVelocity` clamp; it widened the frame to `0x100`, worsened the focused object score to `CURRENT (3035)`, and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` `var_f14` declaration-initializer probe (`f32 var_f14 = 0.0f`); it left the focused object score unchanged at `CURRENT (2550)`, kept early zero in `$f16`, and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` `segmentZVelocity` carry-through probe for the misc-asset speed factor; it worsened the focused object score to `CURRENT (2800)`, moved the key speed/gravity float family from `$f14` to `$f16`, and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` `f64 var_f14` probe; it widened the frame to `0x100`, worsened the focused object score to `CURRENT (10933)`, and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` chained grounded-wheel zeroing probe (`racer->unk88 = (racer->unk84 = 0.0f)`); it produced no object change and left the focused object score unchanged at `CURRENT (2550)`.
- Do not repeat this session's `func_80049794` grounded-wheel zeroing through `var_f0` probe (`var_f0 = 0.0f; racer->unk84 = var_f0; racer->unk88 = var_f0`); it produced no object change, left the focused object score unchanged at `CURRENT (2550)`, and kept early zero in `$f16` instead of target `$f14`.
- Do not repeat this session's `func_80049794` initialized no-op steer-vel probe (`gCurrentCarSteerVel = (updateRateF > 0.0f) * 0`); it improved to `CURRENT (2490)` like the old `FAKEMATCH` hint but remained nonmatching and still lacked target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` pre-`sqrtf` `var_f20` accumulation probe, with or without removing leading `pad5`/`pad7`; it introduced `$f20/$f21` saves but worsened to `CURRENT (4049)`, and the no-pad variant still worsened to `CURRENT (3932)`.
- Do not repeat this session's `func_80049794` pre-`sqrtf` `spEC` sum probe; it worsened to `CURRENT (3300)` and still did not introduce target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` one-pad pre-`sqrtf` `var_f20` accumulation variants; retaining only `pad5` or only `pad7` still widened the frame to `0x100` and worsened to `CURRENT (4125)` despite adding `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` trailing-pad pre-`sqrtf` `var_f20` accumulation variants; removing both trailing pads kept target `0xf8`/`$f20/$f21` but remained nonmatching at `CURRENT (3620)`, and either one-pad trailing variant widened the frame to `0x100` and regressed to `CURRENT (4049)`.
- Do not repeat this session's `func_80049794` both-trailing-pads-removed partial-sum scratch variants (`spCC = x*x + z*z; var_f20 = spCC + y*y` or the same through `spD8`); they kept target `0xf8`/`$f20/$f21` and nudged the focused score to `CURRENT (3615)`, but remained nonmatching with the same early zero, wave-register, and later scheduling drift.
- Do not repeat this session's `func_80049794` both-trailing-pads-removed post-`sqrtf` `var_f2` dataflow probe (`var_f2 = sqrtf(var_f20) - 2.0; var_f20 = var_f2`); it left the focused score unchanged at `CURRENT (3620)`.
- Do not repeat this session's `func_80049794` both-trailing-pads-removed y-first pre-`sqrtf` accumulation probe (`var_f20 = y*y; var_f20 += x*x + z*z`); it worsened the focused score to `CURRENT (3640)` and disturbed the first speed-magnitude velocity load/register order.
- Do not repeat this session's `func_80049794` buoyancy `var_f0` carrier probe (`var_f0 = -1.0f; ... var_f20 = var_f0 - (var_f2 / 10)`); it left the promoted focused score unchanged at `CURRENT (2550)`.
- Do not repeat this session's `func_80049794` simple player-index condition branch-shape probes (`PLAYER_COMPUTER == var_v0` or `if (PLAYER_COMPUTER != var_v0) {} else if (...)`); both left the focused score unchanged at `CURRENT (2550)` and did not swap the target/current branch operands.
- Do not repeat this session's `func_80049794` staged branch-constant probe (`var_v1 = PLAYER_COMPUTER; if ((var_v0 == var_v1) && (gCurrentPlayerIndex != var_v1))`); it left the focused score unchanged at `CURRENT (2550)`, did not swap branch operands, and did not move the `$f14/$f20` allocation.
- Do not repeat this session's `func_80049794` combined leading/trailing pad pre-`sqrtf` accumulation variant; removing `pad5`/`pad7` plus `pad3`/`pad4` shrank the frame to `0xf0` and scored `CURRENT (3737)`.
- Do not repeat this session's `func_80049794` combined leading-pad declaration (`UNUSED s32 pad[2]`); it produced no object change and left the focused score unchanged at `CURRENT (2550)`.
- Do not repeat this session's `func_80049794` double-literal grounded-wheel zero stores (`racer->unk84 = 0.0; racer->unk88 = 0.0`); it worsened the focused score to `CURRENT (4685)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` integer-literal grounded-wheel zero stores (`racer->unk84 = 0; racer->unk88 = 0`); it also worsened the focused score to `CURRENT (4685)` and split the zero stores across `$f4`/`$f6`.
- Do not repeat this session's `func_80059208` partial-object-dot split (`pad = splinePos * diffX; pad2 = -checkpointDot; distance = obj->trans.z_position; pad += diffZ * distance`); it produced no object change and left the focused object score unchanged at `CURRENT (870)`.
- Do not repeat this session's `func_80059208` staged axis-negation probe; it worsened the focused score to `CURRENT (974)`.
- Do not repeat this session's `func_80049794` staged `racerTrickType` local probe; it left the focused score unchanged at `CURRENT (2550)`.
- Do not repeat this session's `func_80049794` post-call `if (var_f20) {}` lifetime no-op; it worsened the focused object score to `CURRENT (3045)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` first zero-clamp `0.0f` literal probe; it worsened the focused object score to `CURRENT (3355)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` early `var_f14 = racer->velocity` lifetime probe; it worsened the relinked focused score to `CURRENT (5000)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` inverse-fraction `var_f2 = var_f20 / 4.0; var_f20 = 1.0 - var_f2` probe; it worsened the relinked focused score to `CURRENT (3850)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` explicit `(s32) var_f14` misc-asset index cast probe; it left the focused object score unchanged at `CURRENT (2550)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` default `spD0` before `spD4` constant-order probe; it left the focused object score unchanged at `CURRENT (2550)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` `register f32 gravity` early-gravity local probe; it widened the frame to `0x100`, worsened the focused object score to `CURRENT (2959)`, and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` `volatile f32 var_f20` probe; it worsened the focused object score to `CURRENT (6441)` and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80049794` direct `f64 var_f20` probe; it widened the frame to `0x108`, worsened the focused object score to `CURRENT (10417)`, and still did not add target `$f20/$f21` saves.
- Do not repeat this session's `func_80059208` `register f32 tempX`/`tempZ` lifetime hints; they produced no object change and left the focused object score unchanged at `CURRENT (870)`.
- Do not repeat this session's `func_80059208` `volatile f32 pad` probe; it worsened the focused object score to `CURRENT (1695)`.
- Do not repeat this session's `func_80059208` final-sum `pad = pad2 + objectDot; diffX = -(pad / divisor)` probe; it left the focused object score unchanged at `CURRENT (870)`.
- Do not repeat this session's `func_80059208` final `pad3 = 5.0f` clamp-local probe; it worsened the focused object score to `CURRENT (1015)`.
- Do not repeat this session's `func_80059208` final vertical numerator `pad3` probe (`pad3 = obj->trans.y_position - tempY; diffY = pad3 / divisor`); it worsened the focused object score to `CURRENT (1680)`.
- Do not repeat this session's `func_80059208` `distance` axis-swap temporary probe; it worsened the focused object score to `CURRENT (1548)`.
- Do not repeat this session's `func_80059208` final vertical numerator `tempY` probe (`tempY = obj->trans.y_position - tempY; diffY = tempY / divisor`); it worsened the focused object score to `CURRENT (1650)`.
- Do not repeat this session's `func_80059208` `scale` axis-swap temporary probe (`scale = diffX; diffX = diffZ; diffZ = -scale`); it worsened the focused object score to `CURRENT (1698)`.
- Do not repeat this session's `func_80059208` `splinePos` axis-swap temporary probe (`splinePos = diffX; diffX = diffZ; diffZ = -splinePos`); it worsened the focused object score to `CURRENT (1698)`.
- Do not repeat this session's `func_80059208` final numerator-negation probe (`diffX = (-(pad + pad2)) / divisor`); it worsened the focused object score to `CURRENT (1600)`.
- Do not repeat this session's `func_80059208` final vertical numerator split through `diffY` (`diffY = obj->trans.y_position; diffY -= tempY; diffY /= divisor`); it worsened the focused object score to `CURRENT (1242)`.
- Do not repeat this session's `func_80059208` checkpoint-dot accumulation probe (`pad2 = tempZ * diffZ; pad2 += diffX * tempX; pad2 = -pad2`); it left the focused object score unchanged at `CURRENT (870)`.
- Do not repeat this session's `func_8002B0F4` pre-call `XInInt`/`ZInInt` or volatile local `LevelModel *levelModel` reload probes; both left the linked score unchanged at `CURRENT (2780)`.
- Do not repeat this session's `trackbg_render_flashy` `scaledXSin`/`scaledXCos` declaration-order or `register` local hints; they produced no object change.
- Do not repeat this session's `trackbg_render_flashy` `zPositions[3] = scaledXCos + (xSin * 1280.0f)` probe; it worsened to linked `CURRENT (5579)` by adding an extra `swc1 $f0, 0x110(sp)` spill and shifting later scheduling/global offsets.
- Do not repeat the prior `func_80059208` positive-checkpoint-dot fold variants; both left the focused score unchanged at `CURRENT (870)`.
- Do not repeat the prior `func_80059208` relative-position lateral dot-product rewrite; it worsened the focused score to `CURRENT (1897)`.
- Do not repeat the prior dedicated early `func_80049794` `f32 gravity` lifetime split; it widened the frame to `0x100` and worsened the focused score to `CURRENT (2959)`.
- The selector-recommended `func_80049794` remains active; the broad `$f20/$f21` allocation split is still unresolved.
- Step: Continue with selector-recommended `func_80049794`, but do not repeat the rejected allocation, wave-scan, early-zero, chained early-zero assignment, early-zero through `var_f0`, double-literal or integer-literal grounded-wheel zero stores, initialized no-op steer-vel assignment, pre-`sqrtf` `var_f20` accumulation with or without removing `pad5`/`pad7`, one-pad pre-`sqrtf` `var_f20` accumulation, trailing-pad pre-`sqrtf` accumulation toggles, combined leading/trailing pad pre-`sqrtf` accumulation, combined leading-pad declaration, pre-`sqrtf` `spEC` sum, dedicated early `f32 zero` local, `var_f14` declaration initializer, `f64 var_f14`, `segmentZVelocity` carry-through, `spA3`, course-height split, inverse-gravity rewrite, inverse-gravity `0.25` multiplication, inverse-fraction `var_f2`, boss-adjustment algebra rewrite, first upper-clamp `4.0f` literal probe, plain or register `f64 speedMagnitude` local, dedicated early-gravity local, `register f32 gravity` early-gravity local, `volatile f32 var_f20`, direct `f64 var_f20`, staged `racerTrickType` local, post-call `if (var_f20) {}` lifetime no-op, first zero-clamp `0.0f` literal probe, early `var_f14` lifetime, explicit `(s32) var_f14` misc-asset index cast, default `spD0` before `spD4` constant-order, combined-register, `spCC` zero, two-step first `sqrtf`, post-`sqrtf` `var_f2` dataflow, `register f32 var_f2`, empty-`racerSteerAngle` lifetime-hint removal, split top-speed multiply, `racerMiscAssetIdx` fractional-reuse, misc-asset interpolation-order, single-load velocity, explicit velocity-clamp float-constant, explicit buoyancy-constant, split buoyancy `-1.0f` materialization, early-`var_f20` lifetime, or `register f32 var_f20` plus `register f32 racerVelocity` probes above. Next useful angle is likely not another simple local/register hint; inspect a different source-shape family around the `$f14/$f16` early-zero split or the first section that actually wants `$f20`, or inspect the remaining local-slot/scheduling drift from the best both-trailing-pads-removed pre-`sqrtf` candidate, and confirm against a relinked focused diff or focused object diff plus full gate. `func_80059208` remains a valid close alternate, but do not repeat the rejected final-offset expression/load-order probes above, including inline-X-only, inline-Z-only, checkpoint-dot `pad3`, object-dot `pad3`, checkpoint-dot `diffY`, `register f32 tempX`/`tempZ`, `volatile f32 pad`, positive `pad2` with direct `(pad2 - pad)`, axis-swap `pad3`, axis-swap `distance`, axis-swap `scale`, axis-swap `splinePos`, final numerator negation, final vertical numerator split through `diffY`, negated-axis-first `diffY`, staged axis negation, nested `pad`/`pad2`, `pad2 += pad` final-sum, `pad = pad2 + objectDot` final-sum, final vertical numerator `pad3`, final vertical numerator `tempY`, `scale = 5.0f` or `pad3 = 5.0f` clamp-limit locals, final `CLAMP` macro forms, relative-position lateral dot-product rewrite, or the positive-checkpoint-dot fold forms. `trackbg_render_flashy` also remains active, but do not repeat the rejected first-four `scaledXSin`, index 5-8 store reorder, `xPositions[2]` operand flip, `xPositions[2] = scaledXCos + scaledXSin`, outer-ring additive-double, named `negScaledXCos`, `scaledXSin`/`scaledXCos` declaration-order or `register` hints, or `zPositions[3] = scaledXCos + (xSin * 1280.0f)` probes above. `func_8002B0F4` remains active too, but do not repeat the empty `gCurrentLevelModel` branch or segment/bounding-box setup-order swap probes. Accept only after `gmake -j4 CROSS=tools/binutils/mips64-elf-` reports `Verify: OK`. Keep `func_80049794`, `func_80059208`, `trackbg_render_flashy`, and `func_8002B0F4` active rather than parked.
